name: CI

on:
  push:
    branches: [ develop, main ]
  workflow_dispatch:
    inputs:
        environment:
          description: "Cél környezet (dev/staging/prod)"
          required: false
          default: "dev"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4